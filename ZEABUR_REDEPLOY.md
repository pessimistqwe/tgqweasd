# 🚀 Восстановление деплоя на Zeabur

## Проблема
Статус деплоя: **REMOVED**

## Решение

### Шаг 1: Проверьте GitHub
Изменения запушены в коммит `27ea377`:
- ✅ Исправлен `Dockerfile` (PYTHONPATH, HEALTHCHECK)
- ✅ Исправлен `start.sh` (лог PORT)
- ✅ Упрощён `zeabur.json`

### Шаг 2: Проверьте Zeabur Dashboard

1. **Зайдите на https://zeabur.com**
2. **Найдите проект `eventpredict`**
3. **Проверьте статус:**
   - 🟡 **Building** — идёт сборка (2-5 минут)
   - 🟢 **Running** — приложение работает ✅
   - 🔴 **Failed** — ошибка деплоя
   - ⚪ **Removed** — деплой удалён

### Шаг 3: Если статус REMOVED

**Вариант A: Redeploy**
1. В Dashboard выберите проект
2. Нажмите **"Redeploy"** или **"Deploy"**
3. Дождитесь завершения сборки

**Вариант B: Создать новый сервис**
1. В проекте нажмите **"New Service"**
2. Выберите **"GitHub"**
3. Выберите репозиторий `pessimistqwe/tgqweasd`
4. Zeabur автоматически определит Docker проект
5. Нажмите **"Deploy"**

### Шаг 4: Проверьте переменные окружения

В Dashboard → Variables убедитесь что есть:

| Ключ | Значение |
|------|----------|
| `PORT` | `8000` |
| `TEST_DB_PATH` | `/tmp/events.db` |
| `PYTHONPATH` | `/app:/app/api` |
| `PYTHONUNBUFFERED` | `1` |

**Опционально:**
| `TELEGRAM_BOT_TOKEN` | ваш токен |
| `ADMIN_TELEGRAM_ID` | ваш ID |

### Шаг 5: Проверьте логи

В Dashboard → Logs ищите:

**Успешный запуск:**
```
Starting EventPredict API...
PORT=8000
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

**Ошибки:**
```
ModuleNotFoundError - проверьте requirements-minimal.txt
ImportError - проверьте PYTHONPATH
Address already in use - проверьте PORT
```

### Шаг 6: Тестирование

**Health check:**
```bash
curl https://YOUR-APP.zeabur.app/health
```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "sync": {...}
}
```

**Frontend:**
```
https://YOUR-APP.zeabur.app
```

---

## 🔧 Если не работает

### Ошибка 502 Bad Gateway

**Причина:** Приложение не запустилось на правильном порту

**Решение:**
1. Проверьте логи на наличие `PORT=8000`
2. Убедитесь что `EXPOSE 8000` в Dockerfile
3. Проверьте что uvicorn запускается на `--port ${PORT}`

### Ошибка 404 Not Found

**Причина:** Неправильный путь к API

**Решение:**
1. Проверьте что `PYTHONPATH=/app:/app/api`
2. Проверьте что `start.sh` использует `api.index:app`

### Health check failed

**Причина:** Health endpoint недоступен

**Решение:**
1. Проверьте что `/health` endpoint существует в `api/index.py`
2. Проверьте логи на наличие ошибок при старте

---

## 📞 Поддержка Zeabur

- **Discord:** https://discord.gg/zeabur
- **Docs:** https://docs.zeabur.com
- **Status:** https://status.zeabur.com

---

**Дата обновления:** 21 февраля 2026
**Коммит:** `27ea377`
