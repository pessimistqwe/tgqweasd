# 🚀 Восстановление деплоя на Zeabur

## Проблема
Статус деплоя: **REMOVED**

## ✅ Решение - 3 шага

### Шаг 1: Проверьте что код запушен

Последний коммит: `e1f1ef1` - fix: Zeabur стабильность

```bash
git log -n 3 --oneline
# Должно показать e1f1ef1
```

### Шаг 2: Создайте НОВЫЙ сервис на Zeabur

**НЕ пытайтесь восстановить старый REMOVED деплой!**

1. **Зайдите на https://zeabur.com**
2. **Выберите ваш проект** (или создайте новый)
3. **Нажмите "New Service"**
4. **Выберите "GitHub"**
5. **Найдите репозиторий** `pessimistqwe/tgqweasd`
6. **Выберите его** - Zeabur автоматически определит Docker проект
7. **Нажмите "Deploy"**

### Шаг 3: Проверьте переменные окружения

В Dashboard → Variables **сразу после создания**:

| Ключ | Значение | Обязательно |
|------|----------|-------------|
| `PORT` | `8000` | ✅ Да |
| `PYTHONPATH` | `/app:/app/api` | ✅ Да |
| `TEST_DB_PATH` | `/tmp/events.db` | ✅ Да |
| `PYTHONUNBUFFERED` | `1` | ✅ Да |

**Опционально (для функционала):**
| `TELEGRAM_BOT_TOKEN` | ваш токен бота |
| `ADMIN_TELEGRAM_ID` | ваш Telegram ID |
| `POLYMARKET_SYNC_INTERVAL` | `7200` |

---

## 🔍 Проверка успешного деплоя

### 1. Проверьте логи

В Dashboard → Logs ищите:

**✅ Успешный запуск:**
```
========================================
Starting EventPredict API...
PORT: 8000
PYTHONPATH: /app:/app/api
TEST_DB_PATH: /tmp/events.db
========================================
Running: python -m uvicorn api.index:app --host 0.0.0.0 --port 8000
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

**❌ Ошибки:**
```
ModuleNotFoundError - не хватает зависимости
ImportError - проблема с импортом
Address already in use - порт занят
```

### 2. Health check

Откройте в браузере:
```
https://YOUR-APP.zeabur.app/health
```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "sync": {
    "last_sync": "...",
    "total_synced": 100,
    "last_error": null
  }
}
```

### 3. Frontend

Откройте:
```
https://YOUR-APP.zeabur.app
```

Должна загрузиться главная страница с рынками.

---

## 🔧 Troubleshooting

### Ошибка 502 Bad Gateway

**Причина:** Приложение не запустилось

**Решение:**
1. Проверьте логи на наличие ошибок
2. Убедитесь что `PORT=8000` в переменных
3. Проверьте что есть `PYTHONPATH=/app:/app/api`

### Ошибка 404 Not Found

**Причина:** Неправильный путь к API

**Решение:**
1. Проверьте `PYTHONPATH=/app:/app/api`
2. Проверьте что `start.sh` использует `api.index:app`

### Деплой падает сразу после старта

**Причина:** Ошибка в коде при старте

**Решение:**
1. Проверьте логи
2. Локально запустите: `docker build -t test . && docker run -p 8000:8000 test`

---

## 📞 Поддержка

- **Zeabur Discord:** https://discord.gg/zeabur
- **Docs:** https://docs.zeabur.com

---

**Последнее обновление:** 21 февраля 2026  
**Коммит:** `e1f1ef1`
