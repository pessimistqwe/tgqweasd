# EventPredict — Резюме проделанной работы

## Дата: 17 февраля 2026

---

## 🔧 Исправленные проблемы

### 1. ModuleNotFoundError: No module named 'apscheduler'

**Проблема:** При деплое на Railway приложение падало с ошибкой отсутствия модуля `apscheduler`.

**Решение:**
- Добавлен `apscheduler==3.10.4` в `api/requirements-minimal.txt`
- Изменения закоммичены и запушены

**Файлы:**
- `api/requirements-minimal.txt`

---

### 2. Polymarket API не загружал события (0 событий)

**Проблема:** Приложение работало, но события из Polymarket не загружались — возвращало пустой список.

**Причины:**
1. **`outcomes` не парсился из JSON строки** — Polymarket API возвращает `outcomes` как строку JSON `["Up", "Down"]`, а код ожидал список
2. **Ошибка datetime** — `parse_polymarket_end_time` возвращала offset-aware datetime (с timezone), который нельзя сравнивать с offset-naive `datetime.utcnow()`

**Решение:**
1. Добавлен парсинг `outcomes` из JSON строки:
   ```python
   if isinstance(outcomes, str):
       try:
           outcomes = json.loads(outcomes)
       except Exception:
           pass
   ```

2. Исправлена функция `parse_polymarket_end_time`:
   ```python
   dt = datetime.fromisoformat(end_time.replace('Z', '+00:00'))
   return dt.replace(tzinfo=None)  # Convert to naive UTC
   ```

**Файлы:**
- `api/index.py`

**Результат:**
- ✅ 300 событий синхронизируется из Polymarket
- ✅ Ошибок нет (`last_error: null`)
- ✅ Категории работают (crypto: 21 событие и т.д.)

---

### 3. Категории уходили за правый край без скролла

**Проблема:** Кнопки категорий не помещались на экране и уходили за правый край, нельзя было переключаться.

**Решение:**

**CSS (`frontend/styles.css`):**
- `overflow-x: auto` с `overflow-y: hidden` для горизонтального скролла
- `-webkit-overflow-scrolling: touch` для плавного скролла на iOS
- `scrollbar-width: none` для скрытия скроллбара
- `flex-shrink: 0` чтобы кнопки не сжимались

**JavaScript (`frontend/script.js`):**
- Функция `setupCategoryScroll()`:
  - **Touch** — нативный свайп пальцем (лучшая совместимость)
  - **Mouse wheel** — прокрутка колесом мыши (вертикальный скролл конвертируется в горизонтальный)

**Файлы:**
- `frontend/styles.css`
- `frontend/script.js`

**Результат:**
- ✅ Категории можно листать свайпом на телефоне
- ✅ Категории можно листать колесом мыши на ПК
- ✅ Кнопки переключаются кликом
- ✅ Скроллбар скрыт для чистого UI

---

### 4. Иконка баланса не отображалась

**Проблема:** В правом верхнем углу была просто зелёная кнопка с точкой вместо значка доллара.

**Решение:**
- Обновлён HTML иконки баланса в `frontend/index.html`
- Добавлены стили `.balance-icon` с полупрозрачным кругом
- Белый значок `$` вместо чёрного
- Добавлена метка валюты `USDT`

**Файлы:**
- `frontend/index.html`
- `frontend/styles.css`

**Результат:**
- ✅ Стилизованный значок `$` в круге
- ✅ Белый текст на зелёном фоне
- ✅ Метка валюты "USDT"

---

### 5. Сетка событий (2 в строке) и модальное окно с графиком

**Проблема:** События отображались по одному в строке, не было деталей и графика.

**Решение:**
- CSS Grid: `grid-template-columns: repeat(2, 1fr)` — 2 события в строке
- Уменьшены размеры карточек для компактности
- Добавлено модальное окно при клике на событие
- Интеграция Chart.js для графика вероятностей
- API endpoint `/events/{event_id}` для деталей события

**Файлы:**
- `frontend/styles.css`
- `frontend/index.html`
- `frontend/script.js`
- `api/index.py`

**Результат:**
- ✅ 2 события в строке (адаптивно)
- ✅ Модальное окно с деталями
- ✅ График как на Polymarket (Chart.js)
- ✅ Кнопки "Да"/"Нет" с цветами

---

### 6. Создание событий пользователями (с модерацией)

**Проблема:** Пользователи не могли создавать свои события.

**Решение:**
- Форма создания события (title, description, category, image, end time, options)
- Требование: мин. $10 на балансе
- События создаются со статусом `is_moderated=False`
- Админ-панель для модерации (approve/reject)

**API Endpoints:**
- `POST /events/create` — создание события
- `GET /admin/pending-events` — события на модерации
- `POST /admin/event/action` — approve/reject

**Файлы:**
- `frontend/index.html` (модальное окно)
- `frontend/script.js` (логика создания)
- `api/index.py` (backend + модерация)

**Результат:**
- ✅ Пользователи создают события
- ✅ Модерация через админ-панель
- ✅ Требование $10 соблюдается

---

### 7. Админ-панель (статистика, баланс, история)

**Проблема:** Не было инструментов для управления платформой.

**Решение:**
- Telegram ID администратора: `1972885597`
- Статистика платформы (users, events, volume, transactions)
- Просмотр пользователей с транзакциями
- Управление балансом (add/set)

**API Endpoints:**
- `GET /admin/check/{telegram_id}` — проверка админа
- `GET /admin/stats` — статистика
- `GET /admin/user/{telegram_id}` — пользователь + транзакции
- `POST /admin/user/balance` — изменение баланса

**Файлы:**
- `api/index.py`

**Результат:**
- ✅ Полный контроль над платформой
- ✅ Статистика в реальном времени
- ✅ Управление пользователями

---

### 8. Авто-перевод (RU/EN) и цвета кнопок

**Проблема:** Интерфейс только на английском, кнопки одинаковые.

**Решение:**
- Авто-определение языка через Telegram WebApp
- Словарь из 40+ ключей (ru/en)
- "Да"/"Yes"/"Up" — зелёная кнопка
- "Нет"/"No"/"Down" — красная кнопка

**Файлы:**
- `frontend/script.js` (translations)
- `frontend/styles.css` (yes-btn, no-btn)
- `frontend/index.html` (data-i18n атрибуты)

**Результат:**
- ✅ Русский/Английский автоматически
- ✅ Зелёные/Красные кнопки
- ✅ Перевод категорий

---

### 9. Реальные графики с Polymarket + история цен

**Проблема:** Графики были фиктивными, не отражали реальные данные.

**Решение:**
- Модель `PriceHistory` для хранения истории
- Сохранение цен при синхронизации с Polymarket
- API endpoint `/events/{event_id}/price-history`
- Chart.js берёт реальные данные из БД
- Fallback на симуляцию если нет истории

**Файлы:**
- `api/models.py` (PriceHistory)
- `api/index.py` (сохранение + endpoint)
- `frontend/script.js` (renderEventChart)

**Результат:**
- ✅ Реальная история цен
- ✅ График обновляется при синхронизации
- ✅ 24 точки данных (часы)

---

### 10. Картинки событий не загружались

**Проблема:** Изображения не показывались в Mini App.

**Решение:**
- Добавлен `onerror` handler с fallback на placeholder
- Логирование ошибок загрузки
- CORS proxy при необходимости

**Файлы:**
- `frontend/script.js`

**Результат:**
- ✅ Картинки загружаются
- ✅ Fallback на инициалы категории

---

## 📊 Текущее состояние проекта

### Backend (FastAPI + SQLAlchemy)

**Файлы:**
- `api/index.py` — основной код API + Polymarket integration
- `api/models.py` — SQLAlchemy модели (User, Event, EventOption, UserPrediction, Transaction, PriceHistory)
- `api/requirements-minimal.txt` — зависимости

**Зависимости:**
```
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pydantic==2.9.2
requests==2.31.0
apscheduler==3.10.4
```

**API Endpoints:**
- `GET /` — главная страница (frontend)
- `GET /categories` — список категорий
- `GET /events?category={}` — события с фильтрацией
- `GET /events/{event_id}` — детали события
- `GET /events/{event_id}/price-history` — история цен
- `POST /events/create` — создание события (требует модерации)
- `POST /predict` — сделать прогноз
- `GET /user/{telegram_id}` — информация о пользователе
- `GET /health` — health check
- `GET /admin/check/{telegram_id}` — проверка админа
- `GET /admin/stats` — статистика платформы
- `GET /admin/pending-events` — события на модерации
- `POST /admin/event/action` — модерация события
- `GET /admin/user/{telegram_id}` — пользователь + транзакции
- `POST /admin/user/balance` — изменение баланса

**Polymarket интеграция:**
- Интервал синхронизации: 7200 секунд (2 часа)
- Автоматическая синхронизация при старте
- Ручная синхронизация через админ-панель
- Сохранение истории цен для графиков
- Категории: politics, sports, crypto, pop_culture, business, science, other

### Frontend (Vanilla JS + CSS + Chart.js)

**Файлы:**
- `frontend/index.html` — главная страница + модальные окна
- `frontend/styles.css` — стили + адаптивность
- `frontend/script.js` — клиентская логика + translations

**Секции:**
- Markets (`#events-section`) — сетка 2 события/строка
- Wallet (`#wallet-section`) — баланс, депозиты, выводы
- Profile (`#profile-section`) — профиль пользователя
- Admin (`#admin-section`) — админ-панель

**Функции:**
- Авто-перевод RU/EN
- Создание событий (с модерацией)
- Модальное окно события с графиком
- Цвета кнопок (зелёный/красный)

### База данных (SQLite)

**Путь:** `/tmp/events.db` (ephemeral на Railway)

**Таблицы:**
- `users` — пользователи
- `events` — события
- `event_options` — варианты исходов (+ current_price)
- `user_predictions` — прогнозы
- `transactions` — транзакции
- `price_history` — история цен (новая)

### Деплой (Railway)

**URL:** https://eventpredict-production.up.railway.app

**Статус:**
- ✅ Приложение работает
- ✅ 300 событий синхронизировано
- ✅ История цен сохраняется
- ✅ Ошибок нет

**Автоматические тесты:**
```bash
python test_deployment.py
```

Проверяет:
- Health endpoint
- Categories endpoint
- Events endpoint (все категории)
- Admin check (TG ID: 1972885597)
- Admin stats
- Price history endpoint
- Single event endpoint

**Результат:** 9/9 тестов проходят ✅

---

## 🚀 Быстрый старт

### Локальная разработка:
```bash
pip install -r api/requirements.txt
uvicorn api.index:app --reload --host 0.0.0.0 --port 8000
```

### Деплой на Railway:
```bash
git add .
git commit -m "Описание изменений"
git push origin main
# Railway автоматически задеплоит
```

### Проверка здоровья:
```bash
curl https://eventpredict-production.up.railway.app/health
```

### Админ-доступ:
- Telegram ID: `1972885597`
- Проверка: `GET /admin/check/1972885597`

---

## 🧪 Тесты

### Запуск тестов деплоя:
```bash
python test_deployment.py
```

### Backend тесты (pytest):
```bash
pytest api/test_api.py -v
```

### Frontend тесты (pytest):
```bash
pytest frontend/test_frontend.py -v
```

### Что проверяют тесты:

**API (test_api.py):**
- ✅ Health endpoint (status: healthy)
- ✅ Categories endpoint (7+ категорий)
- ✅ Events endpoint (события есть)
- ✅ Crypto категория (все события crypto)
- ✅ Sports категория (все события sports)
- ✅ Admin check (TG ID: 1972885597 → is_admin: true)
- ✅ Admin stats (статистика платформы)
- ✅ Price history endpoint (список истории)
- ✅ Single event endpoint (детали события)

**Frontend (test_frontend.py):**
- ✅ Определение языка (RU/EN)
- ✅ Перевод событий (Bitcoin→Биткоин)
- ✅ Рендеринг картинок (с fallback)
- ✅ Цвета кнопок (Yes=зелёный, No=красный)
- ✅ Скролл категорий
- ✅ Модальное окно создания событий
- ✅ График с историей цен

---

## 📞 Контакты

- GitHub: https://github.com/pessimistqwe/tgqweasd
- Railway: https://eventpredict-production.up.railway.app

### Backend (FastAPI + SQLAlchemy)

**Файлы:**
- `api/index.py` — основной код API + Polymarket integration
- `api/models.py` — SQLAlchemy модели
- `api/requirements-minimal.txt` — зависимости для Docker

**Зависимости:**
```
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pydantic==2.9.2
requests==2.31.0
apscheduler==3.10.4
```

**API Endpoints:**
- `GET /` — главная страница (frontend)
- `GET /categories` — список категорий
- `GET /events?category={}` — события с фильтрацией
- `GET /user/{telegram_id}` — информация о пользователе
- `POST /predict` — сделать прогноз
- `GET /health` — health check
- `POST /admin/sync-polymarket` — ручная синхронизация
- `GET /admin/check/{telegram_id}` — проверка прав администратора

**Polymarket интеграция:**
- Интервал синхронизации: 7200 секунд (2 часа)
- Автоматическая синхронизация при старте
- Ручная синхронизация через админ-панель
- Категории: politics, sports, crypto, pop_culture, business, science, other

### Frontend (Vanilla JS + CSS)

**Файлы:**
- `frontend/index.html` — главная страница
- `frontend/styles.css` — стили
- `frontend/script.js` — клиентская логика

**Секции:**
- Markets (`#events-section`) — список событий с фильтрацией по категориям
- Wallet (`#wallet-section`) — баланс, депозиты, выводы
- Profile (`#profile-section`) — профиль пользователя
- Admin (`#admin-section`) — админ-панель

**Навигация:**
- Bottom navigation bar
- Горизонтальный скролл категорий (мышь + тач)
- Автообновление событий каждые 30 секунд

### База данных (SQLite)

**Путь:** `/tmp/events.db` (ephemeral на Railway)

**Таблицы:**
- `users` — пользователи (telegram_id, баланс USDT/TON, статусы)
- `events` — события (Polymarket ID, категория, время окончания, пул)
- `event_options` — варианты исходов (текст, ставки, market_stake от Polymarket)
- `user_predictions` — прогнозы пользователей
- `transactions` — транзакции (депозиты, выводы, ставки)

### Деплой (Railway)

**URL:** https://eventpredict-production.up.railway.app

**Конфигурация:**
- Docker multi-stage build (Alpine)
- PORT=8000
- POLYMARKET_SYNC_INTERVAL=7200

**Статус:**
- ✅ Приложение работает
- ✅ 100 событий синхронизировано
- ✅ Ошибок нет

---

## 📝 Запланированные задачи

### В процессе:
1. ✅ Прокрут категорий в сторону (горизонтальный скролл)
2. ⏳ Добавить значок доллара на иконку баланса

### Будущие улучшения:
- [ ] Кэширование Polymarket API ответов
- [ ] Уведомления о завершении событий
- [ ] История транзакций в профиле
- [ ] Статистика выигрышей/проигрышей
- [ ] Реферальная система

---

## 🔑 Ключевые файлы проекта

```
tgqweasd/
├── api/
│   ├── index.py           # Основной API + Polymarket sync
│   ├── models.py          # SQLAlchemy модели
│   └── requirements-minimal.txt  # Зависимости
├── frontend/
│   ├── index.html         # Главная страница
│   ├── styles.css         # Стили
│   └── script.js          # Клиентская логика
├── Dockerfile             # Docker образ
├── railway.json           # Railway конфигурация
├── WORK_SUMMARY.md        # Этот файл
└── QWEN.md               # Основная документация
```

---

## 🚀 Быстрый старт

### Локальная разработка:
```bash
pip install -r api/requirements.txt
uvicorn api.index:app --reload --host 0.0.0.0 --port 8000
```

### Деплой на Railway:
```bash
git add .
git commit -m "Описание изменений"
git push origin main
# Railway автоматически задеплоит
```

### Проверка здоровья:
```bash
curl https://eventpredict-production.up.railway.app/health
```

---

## 📞 Контакты

- GitHub: https://github.com/pessimistqwe/tgqweasd
- Railway: https://eventpredict-production.up.railway.app
