# EventPredict — Prediction Market в Telegram

[![Python 3.12+](https://img.shields.io/badge/python-3.12+-blue.svg)](https://www.python.org/downloads/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**EventPredict** — это полноценный prediction market (рынок предсказаний) в формате Telegram Mini App с интеграцией Polymarket API и Binance WebSocket для реальных цен криптовалют.

## 🚀 Возможности

### Для пользователей:
- **Торговля предсказаниями** на события из различных категорий (политика, спорт, крипто, поп-культура, бизнес, наука)
- **Real-time графики** цен на криптовалюты через Binance WebSocket
- **Краткосрочные прогнозы** на 5 минут (вверх/вниз)
- **Long/Short позиции** с кредитным плечом до 100x
- **Polymarket-style ставки** на бинарные события (Yes/No)
- **Кошелёк** с депозитами/выводами через CryptoBot
- **История ставок** и статистика трейдинга

### Для администраторов:
- **Админ-панель** с управлением пользователями и выводами
- **Ручная синхронизация** событий из Polymarket
- **Модерация** событий и комментариев
- **Статистика платформы** в реальном времени

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Структура проекта](#-структура-проекта)
- [API Endpoints](#-api-endpoints)
- [База данных](#-база-данных)
- [Тестирование](#-тестирование)
- [Деплой](#-деплой)
- [Переменные окружения](#-переменные-окружения)
- [Технологический стек](#-технологический-стек)

---

## 🎯 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/eventpredict.git
cd eventpredict
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env и добавьте TELEGRAM_BOT_TOKEN
```

### 4. Запуск сервера разработки

```bash
uvicorn api.index:app --reload --host 0.0.0.0 --port 8000
```

### 5. Открыть в Telegram

Откройте приложение по ссылке: `https://t.me/yourbot?startapp=webapp`

---

## 🛠 Установка и настройка

### Требования

- Python 3.12+
- PostgreSQL (для production) или SQLite (для разработки)
- Telegram Bot Token (от @BotFather)
- CryptoBot API Token (опционально, для платежей)

### Локальная разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Копирование env файла
cp .env.example .env

# Запуск с авто-релоадом
uvicorn api.index:app --reload --host 0.0.0.0 --port 8000
```

### Docker

```bash
# Сборка образа
docker build -t eventpredict .

# Запуск контейнера
docker run -p 8000:8000 \
  -e TELEGRAM_BOT_TOKEN=your_token \
  -e PORT=8000 \
  eventpredict
```

---

## 📁 Структура проекта

```
tgqweasd/
├── api/                        # Backend (FastAPI)
│   ├── index.py               # Основной файл приложения
│   ├── models.py              # SQLAlchemy модели
│   ├── betting_service.py     # Betting Engine (бизнес-логика)
│   ├── betting_repository.py  # Работа с БД
│   ├── betting_routes.py      # API endpoints для ставок
│   ├── betting_resolver.py    # Фоновый воркер для расчёта ставок
│   ├── betting_models.py      # Pydantic модели для ставок
│   ├── telegram_auth.py       # Валидация Telegram initData
│   ├── main.py                # Точка входа для некоторых платформ
│   └── requirements*.txt      # Зависимости Python
├── frontend/                   # Frontend (Vanilla JS)
│   ├── index.html             # Главная страница
│   ├── styles.css             # Стили
│   └── script.js              # Клиентская логика
├── tests/                      # Тесты (pytest)
│   ├── test_betting_service.py
│   ├── test_resolver.py
│   └── conftest.py
├── .env.example               # Шаблон переменных окружения
├── .gitignore                 # Git ignore правила
├── Dockerfile                 # Docker образ
├── zeabur.json               # Конфигурация Zeabur
├── railway.json              # Конфигурация Railway
├── render.yaml               # Конфигурация Render
├── fly.toml                  # Конфигурация Fly.io
├── vercel.json               # Конфигурация Vercel
└── README.md                 # Эта документация
```

---

## 📡 API Endpoints

### Публичные endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/` | Главная страница (frontend) |
| `GET` | `/api/categories` | Список категорий |
| `GET` | `/api/events?category={}` | Получить события |
| `GET` | `/api/user/{telegram_id}` | Информация о пользователе |
| `GET` | `/health` | Health check |

### Betting API (требует Telegram initData)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/betting/event/place` | Разместить ставку на событие |
| `POST` | `/api/betting/price/place` | Разместить ставку на цену (Long/Short) |
| `POST` | `/api/betting/prediction/place` | Краткосрочный прогноз (5 мин) |
| `POST` | `/api/betting/cancel` | Отменить ставку |
| `GET` | `/api/betting/my-bets` | Мои ставки |
| `GET` | `/api/betting/my-predictions` | Мои прогнозы |
| `GET` | `/api/betting/bet/{bet_id}` | Информация о ставке |
| `GET` | `/api/betting/market-stats/{market_id}` | Статистика рынка |

### Кошелёк

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/wallet/balance/{telegram_id}` | Баланс пользователя |
| `POST` | `/api/wallet/deposit` | Создать депозит |
| `POST` | `/api/wallet/withdraw` | Запросить вывод |

### Админка

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/admin/sync-polymarket` | Ручная синхронизация Polymarket |
| `GET` | `/api/admin/check/{telegram_id}` | Проверка прав администратора |
| `GET` | `/api/admin/stats` | Статистика платформы |
| `GET` | `/api/admin/withdrawals` | Список выводов |
| `POST` | `/api/admin/withdrawal/action` | Одобрить/отклонить вывод |

---

## 🗄 База данных

### Таблицы

#### users
- `id` — первичный ключ
- `telegram_id` — Telegram ID (уникальный)
- `username` — имя пользователя
- `balance_usdt` — баланс в USDT
- `balance_ton` — баланс в TON
- `is_blocked` — заблокирован ли пользователь
- `is_admin` — права администратора

#### events (markets)
- `id` — первичный ключ
- `polymarket_id` — ID события в Polymarket
- `title` — заголовок события
- `category` — категория (crypto, politics, sports...)
- `end_time` — время окончания
- `is_active` — активно ли
- `is_resolved` — разрешено ли
- `correct_option` — выигрышный опцион

#### bets
- `id` — первичный ключ
- `user_id` — владелец ставки
- `market_id` — рынок
- `bet_type` — тип (EVENT, PRICE, PRICE_PREDICTION)
- `direction` — направление (YES/NO, LONG/SHORT, UP/DOWN)
- `amount` — сумма ставки
- `shares` — количество акций (для Polymarket-style)
- `entry_price` — цена входа
- `leverage` — кредитное плечо
- `liquidation_price` — цена ликвидации
- `take_profit_price` — тейк-профит
- `stop_loss_price` — стоп-лосс
- `potential_payout` — потенциальный выигрыш
- `actual_payout` — фактический выигрыш
- `status` — статус (pending, open, closed, cancelled)

#### price_predictions
- `id` — первичный ключ
- `user_id` — владелец прогноза
- `market_id` — рынок
- `direction` — LONG/SHORT
- `symbol` — символ актива (BTCUSDT)
- `amount` — сумма
- `odds` — коэффициент
- `entry_price` — цена входа
- `exit_price` — цена выхода
- `duration_seconds` — длительность прогноза
- `status` — статус

#### transactions
- `id` — первичный ключ
- `user_id` — пользователь
- `type` — тип (deposit, withdrawal, bet_placed, bet_won)
- `amount` — сумма
- `status` — статус
- `invoice_url` — ссылка на оплату

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Установка pytest
pip install pytest pytest-cov pytest-asyncio

# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=api --cov-report=html

# Запуск конкретного теста
pytest tests/test_betting_service.py::test_race_condition -v
```

### Обязательные тест-кейсы

| № | Название | Описание |
|---|----------|----------|
| 1 | `test_race_condition` | 2 одновременные ставки с одним балансом |
| 2 | `test_price_manipulation` | Ставка с ценой на 5% выше рыночной |
| 3 | `test_payout_calculation` | Расчет выплаты для Long x10 |
| 4 | `test_state_consistency` | Воркер закрывает ставку по результату |
| 5 | `test_websocket_reconnect` | Обрыв связи с Binance |
| 6 | `test_negative_balance` | Попытка ставки больше баланса |
| 7 | `test_decimal_precision` | Расчет с маленькими суммами |

---

## 🚀 Деплой

### Zeabur (рекомендуется)

1. Создайте аккаунт на [Zeabur](https://zeabur.com)
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `PORT=8000`
   - `TEST_DB_PATH=/tmp/events.db`
4. Deploy!

См. подробную инструкцию в [DEPLOY_ZEABUR.md](./DEPLOY_ZEABUR.md)

### Другие платформы

- **Railway.app** — см. `railway.json`
- **Render.com** — см. `render.yaml`
- **Fly.io** — см. `fly.toml`
- **Vercel** — см. `vercel.json`

---

## 🔐 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `8000` |
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | — |
| `ADMIN_TELEGRAM_ID` | Telegram ID администратора | — |
| `TEST_DB_PATH` | Путь к SQLite БД | `/tmp/events.db` |
| `DATABASE_URL` | PostgreSQL connection string | — |
| `POLYMARKET_SYNC_INTERVAL` | Интервал синхронизации (сек) | `7200` |
| `CRYPTOBOT_API_TOKEN` | Токен CryptoBot | — |
| `SECRET_KEY` | Секретный ключ для сессий | — |

Скопируйте `.env.example` в `.env` и заполните необходимые значения.

---

## 🛠 Технологический стек

| Компонент | Технология |
|-----------|------------|
| **Backend** | Python 3.12, FastAPI 0.104, SQLAlchemy 2.0, Pydantic 2.5 |
| **Frontend** | Vanilla JavaScript, CSS3, Telegram WebApp SDK, Chart.js |
| **База данных** | SQLite (dev/serverless), PostgreSQL (production) |
| **Контейнеризация** | Docker (multi-stage build на Alpine) |
| **Внешние API** | Polymarket Gamma API, Binance WebSocket API |
| **Платежи** | CryptoBot API |
| **Тесты** | pytest, pytest-cov, pytest-asyncio |

---

## 📄 Лицензия

MIT License — см. файл [LICENSE](./LICENSE)

---

## 🤝 Поддержка

- Документация по деплою: [DEPLOY_ZEABUR.md](./DEPLOY_ZEABUR.md)
- API документация: `/docs` (Swagger UI) после запуска
- Telegram Bot: [@yourbot](https://t.me/yourbot)

---

## 📝 Changelog

### v1.0.0
- ✅ Полная реализация Betting Engine
- ✅ Интеграция с Polymarket API
- ✅ Binance WebSocket для реальных цен
- ✅ Telegram WebApp интеграция
- ✅ Админ-панель
- ✅ Тесты (90%+ покрытие)
