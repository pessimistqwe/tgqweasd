# 🚀 Инструкции по деплою EventPredict

## ✅ Рекомендуемые бесплатные платформы (в порядке приоритета)

### 1. **Railway.app** ⭐ (ЛУЧШИЙ ВЫБОР)
- ✅ **$5 бесплатных кредитов/месяц** (хватает для небольшого проекта)
- ✅ Автоматический деплой из GitHub
- ✅ Больше ресурсов чем Zeabur бесплатно
- ✅ Простая настройка

**Как задеплоить:**
1. Зайди на https://railway.app
2. Sign up через GitHub
3. New Project → Deploy from GitHub repo
4. Выбери репозиторий `tgqweasd`
5. Railway автоматически определит Dockerfile
6. В Environment Variables добавь (если нужно):
   - `POLYMARKET_SYNC_INTERVAL=1800`
   - `CRYPTOBOT_API_TOKEN=...` (опционально)
   - `ADMIN_TELEGRAM_IDS=...` (опционально)
7. Готово! Railway даст тебе URL вида `https://xxx.up.railway.app`

---

### 2. **Render.com** ⭐
- ✅ **Бесплатный тариф** с ограничениями (sleep после 15 мин бездействия)
- ✅ Автоматический деплой из GitHub
- ✅ Больше дискового пространства

**Как задеплоить:**
1. Зайди на https://render.com
2. Sign up через GitHub
3. New → Web Service
4. Connect репозиторий `tgqweasd`
5. Настройки:
   - **Build Command:** `pip install -r api/requirements-minimal.txt`
   - **Start Command:** `uvicorn index:app --host 0.0.0.0 --port $PORT --app-dir api`
   - **Environment:** Python 3
6. Add Environment Variables (если нужно)
7. Create Web Service

---

### 3. **Fly.io** ⭐
- ✅ **Бесплатный тариф** с ограничениями (3 shared-cpu-1x VMs)
- ✅ Хорошая производительность
- ✅ Глобальная CDN

**Как задеплоить:**
1. Установи Fly CLI: `iwr https://fly.io/install.ps1 -useb | iex` (PowerShell)
2. Зайди на https://fly.io и sign up
3. В терминале:
   ```bash
   cd C:\Users\user\Desktop\tgqweasd
   fly launch
   ```
4. Следуй инструкциям (выбери регион, имя приложения)
5. `fly deploy` - задеплоит автоматически

---

### 4. **Vercel** (уже настроен)
- ✅ Бесплатный тариф
- ⚠️ Может быть ограничения для FastAPI (serverless functions)
- ✅ Уже есть `vercel.json`

**Как задеплоить:**
1. Зайди на https://vercel.com
2. Import Project → выбери репозиторий
3. Vercel автоматически использует `vercel.json`
4. Deploy

---

## 🔧 Оптимизация для Zeabur (если хочешь остаться)

Я создал **максимально оптимизированный Dockerfile** с:
- ✅ Multi-stage build (Alpine Linux - самый легкий)
- ✅ Минимальные зависимости (`requirements-minimal.txt`)
- ✅ Удаление кэша pip после установки
- ✅ Только нужные файлы в образе

**Размер образа должен быть ~150-200MB вместо 500MB+**

**Что сделать:**
1. Закоммить изменения:
   ```bash
   git add Dockerfile api/requirements-minimal.txt .dockerignore
   git commit -m "Optimize Docker image size for Zeabur free tier"
   git push origin main
   ```
2. В Zeabur → Redeploy
3. Если всё равно не хватает места → переходи на Railway/Render

---

## 📊 Сравнение платформ

| Платформа | Бесплатный тариф | Диск | RAM | CPU | Sleep |
|-----------|----------------|------|-----|-----|-------|
| **Railway** | $5 кредитов/мес | ✅ | 512MB | 1 vCPU | ❌ |
| **Render** | Free | ✅ | 512MB | 0.5 vCPU | ✅ (15 мин) |
| **Fly.io** | 3 VMs | ✅ | 256MB | Shared | ❌ |
| **Zeabur** | Free | ⚠️ Мало | 512MB | Shared | ❌ |
| **Vercel** | Free | ✅ | - | Serverless | ❌ |

---

## 🎯 Рекомендация

**Используй Railway.app** - лучший баланс бесплатных ресурсов и простоты настройки.
