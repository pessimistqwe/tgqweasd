# EventPredict Roadmap

План развития проекта Prediction Market с интеграцией Polymarket и Binance.

---

## 📊 Обзор

| Фаза | Название | Длительность | Статус |
|------|----------|--------------|--------|
| 1 | Стабилизация | 1 неделя | ✅ В работе |
| 2 | Polymarket-механики | 2 недели | ⏳ Ожидает |
| 3 | Событийные рынки | 2 недели | ⏳ Ожидает |
| 4 | Экономика и ликвидность | 2 недели | ⏳ Ожидает |
| 5 | Масштабирование | 4 недели | ⏳ Ожидает |

---

## 🔴 Фаза 1: Стабилизация (1 неделя)

**Цель:** Исправить критические проблемы и улучшить стабильность приложения.

### Задачи

| # | Задача | Приоритет | Статус | Файлы |
|---|--------|-----------|--------|-------|
| 1.1 | Исправить график — загрузка истории + WebSocket | 🔴 Критичный | ✅ Готово | `frontend/useBinanceWebSocket.js`, `frontend/script.js` |
| 1.2 | Исправить расчет коэффициентов от волатильности | 🔴 Критичный | ✅ Готово | `api/volatility_service.py`, `api/volatility_routes.py` |
| 1.3 | Добавить индикатор задержки соединения | 🟡 Средний | ⏳ В работе | `frontend/script.js` |
| 1.4 | Тесты на корректность отображения графика | 🟡 Средний | ⏳ В работе | `tests/test_chart.py` |
| 1.5 | Оптимизация производительности графика | 🟢 Низкий | ⏳ В работе | `frontend/useBinanceWebSocket.js` |

### API Endpoints

- `GET /api/volatility/odds/{symbol}` — получить коэффициент для символа
- `GET /api/volatility/odds?symbols=...` — пакетный запрос коэффициентов
- `GET /api/volatility/cache/{symbol}` — получить закэшированный коэффициент
- `GET /api/volatility/config` — конфигурация сервиса волатильности
- `GET /api/betting/volatility-odds` — коэффициент для прогноза

### Критерии приемки

- [ ] График загружает реальные исторические свечи (не "шаблон")
- [ ] WebSocket обновляет график в реальном времени
- [ ] Коэффициент зависит от волатильности (не от таймфрейма)
- [ ] Индикатор задержки показывает пинг до Binance
- [ ] Тесты проходят успешно

---

## 🟠 Фаза 2: Polymarket-механики (2 недели)

**Цель:** Внедрить полноценную систему акций и книжку ордеров.

### Задачи

| # | Задача | Приоритет | Статус | Файлы |
|---|--------|-----------|--------|-------|
| 2.1 | Система акций (YES/NO shares) вместо Вверх/Вниз | 🔴 Критичный | ⏳ Ожидает | `api/models.py`, `api/betting_models.py` |
| 2.2 | Книжка ордеров (order book) с ценами покупки/продажи | 🔴 Критичный | ⏳ Ожидает | `api/order_book.py`, `frontend/order_book.js` |
| 2.3 | Возможность продать позицию до завершения события | 🔴 Критичный | ⏳ Ожидает | `api/betting_service.py` |
| 2.4 | История всех позиций пользователя с PnL | 🟡 Средний | ⏳ Ожидает | `frontend/positions.js` |
| 2.5 | График PnL позиции в реальном времени | 🟢 Низкий | ⏳ Ожидает | `frontend/chart_pnl.js` |

### Архитектура

```
# Текущая модель (упрощенно)
Bet {
    direction: "long" / "short"
    amount: 10.00
    entry_price: 50000
}

# Новая модель (Polymarket-style)
Position {
    market_id: 1
    outcome: "YES" / "NO"
    shares: 20.5
    average_price: 0.52
    current_value: 10.61  # shares * current_price
    unrealized_pnl: +0.61
}
```

### API Endpoints (новые)

- `GET /api/order-book/{market_id}` — книжка ордеров
- `POST /api/position/sell` — продать позицию
- `GET /api/positions/my` — мои позиции с PnL
- `GET /api/positions/{id}/pnl` — график PnL позиции

### Критерии приемки

- [ ] Пользователи покупают акции YES/NO
- [ ] Видна книжка ордеров с лучшими ценами
- [ ] Можно продать позицию в любой момент
- [ ] История показывает PnL по каждой позиции

---

## 🟡 Фаза 3: Событийные рынки (2 недели)

**Цель:** Интеграция с Polymarket API и создание событийных рынков.

### Задачи

| # | Задача | Приоритет | Статус | Файлы |
|---|--------|-----------|--------|-------|
| 3.1 | Интеграция с Polymarket API для событий | 🔴 Критичный | ⏳ В работе | `api/index.py` (уже есть) |
| 3.2 | Создание своих событий через админ-панель | 🔴 Критичный | ⏳ Ожидает | `api/admin_routes.py` |
| 3.3 | Система разрешения событий (оракул) | 🔴 Критичный | ⏳ Ожидает | `api/oracle.py` |
| 3.4 | Автоматическое начисление выигрышей | 🔴 Критичный | ⏳ Ожидает | `api/betting_resolver.py` |
| 3.5 | Отображение результатов событий | 🟡 Средний | ⏳ Ожидает | `frontend/results.js` |

### Механика оракула

```python
class Oracle:
    """Система разрешения событий"""
    
    # Источники данных
    sources = [
        "polymarket_api",      # Polymarket resolution
        "manual_admin",        # Ручное разрешение админом
        "chainlink",          # Chainlink Oracle (future)
        "custom_api"          # Кастомный API
    ]
    
    def resolve_event(self, event_id: int):
        # 1. Проверяем Polymarket API
        # 2. Если resolved — получаем результат
        # 3. Обновляем событие в БД
        # 4. Запускаем расчет ставок
```

### API Endpoints (новые)

- `POST /api/admin/event/create` — создать событие
- `POST /api/admin/event/{id}/resolve` — разрешить событие
- `GET /api/events/resolved` — завершенные события
- `GET /api/oracle/sources` — источники оракула

### Критерии приемки

- [ ] События импортируются из Polymarket
- [ ] Админ может создать свое событие
- [ ] Оракул автоматически разрешает события
- [ ] Выигрыши начисляются автоматически

---

## 🟢 Фаза 4: Экономика и ликвидность (2 недели)

**Цель:** Построение устойчивой экономической модели.

### Задачи

| # | Задача | Приоритет | Статус | Файлы |
|---|--------|-----------|--------|-------|
| 4.1 | Пул ликвидности (AMM модель) | 🔴 Критичный | ⏳ Ожидает | `api/amm_pool.py` |
| 4.2 | Комиссия платформы (2–5% с выигрыша) | 🔴 Критичный | ⏳ Ожидает | `api/betting_service.py` |
| 4.3 | Реферальная система | 🟡 Средний | ⏳ Ожидает | `api/referral.py` |
| 4.4 | Вывод средств (крипто или фиат) | 🔴 Критичный | ⏳ Ожидает | `api/withdrawal.py` |
| 4.5 | Статистика доходов платформы | 🟢 Низкий | ⏳ Ожидает | `admin/dashboard.py` |

### AMM Модель

```python
class AMMPool:
    """Automated Market Maker для ликвидности"""
    
    # Формула: x * y = k
    # YES_shares * NO_shares = k
    
    def get_price(self, outcome: str) -> Decimal:
        # Цена = доля исхода в пуле
        total = self.yes_shares + self.no_shares
        if outcome == "YES":
            return self.yes_shares / total
        return self.no_shares / total
    
    def buy_shares(self, outcome: str, amount: Decimal):
        # Покупаем акции по текущей цене
        # Увеличиваем пул, меняем цену
        pass
```

### Структура комиссии

```
Выигрыш: $100
Комиссия платформы: 3% = $3
Пользователь получает: $97
```

### API Endpoints (новые)

- `GET /api/pool/liquidity` — ликвидность пула
- `POST /api/pool/add-liquidity` — добавить ликвидность
- `POST /api/pool/remove-liquidity` — убрать ликвидность
- `GET /api/platform/fees` — статистика комиссий
- `GET /api/referral/code` — реферальный код
- `POST /api/withdraw/request` — запрос на вывод

### Критерии приемки

- [ ] Работает AMM пул ликвидности
- [ ] Комиссия списывается с выигрыша
- [ ] Реферальная система начисляет бонусы
- [ ] Вывод средств работает корректно

---

## 🔵 Фаза 5: Масштабирование (4 недели)

**Цель:** Подготовка к продакшену и масштабированию.

### Задачи

| # | Задача | Приоритет | Статус | Файлы |
|---|--------|-----------|--------|-------|
| 5.1 | Мобильная адаптация UI | 🔴 Критичный | ⏳ Ожидает | `frontend/styles.css` |
| 5.2 | Уведомления о завершении ставок | 🟡 Средний | ⏳ Ожидает | `api/notifications.py` |
| 5.3 | Лидерборд пользователей | 🟢 Низкий | ⏳ Ожидает | `frontend/leaderboard.js` |
| 5.4 | Публичный API для разработчиков | 🟡 Средний | ⏳ Ожидает | `docs/api.md` |
| 5.5 | Rate limiting и защита от атак | 🔴 Критичный | ⏳ Ожидает | `api/middleware.py` |
| 5.6 | Мониторинг и логирование | 🔴 Критичный | ⏳ Ожидает | `api/logging_config.py` |
| 5.7 | CI/CD пайплайн | 🟡 Средний | ⏳ Ожидает | `.github/workflows/` |
| 5.8 | Документация для пользователей | 🟢 Низкий | ⏳ Ожидает | `docs/user_guide.md` |

### Инфраструктура

```
┌─────────────────────────────────────────────┐
│           Load Balancer (Nginx)             │
└─────────────────┬───────────────────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
┌───▼───┐   ┌────▼────┐   ┌───▼───┐
│ App 1 │   │  App 2  │   │ App 3 │
└───┬───┘   └────┬────┘   └───┬───┘
    │            │            │
    └────────────┼────────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
┌───▼───┐   ┌───▼───┐   ┌───▼───┐
│ Redis │   │  DB   │   │ Queue │
└───────┘   └───────┘   └───────┘
```

### API Endpoints (новые)

- `GET /api/leaderboard` — лидерборд пользователей
- `GET /api/public/events` — публичный API событий
- `GET /api/public/stats` — публичная статистика
- `POST /api/webhook/poly market` — вебхук от Polymarket

### Критерии приемки

- [ ] UI адаптирован под мобильные устройства
- [ ] Уведомления приходят в Telegram
- [ ] Публичный API документирован
- [ ] Rate limiting защищает от DDoS
- [ ] CI/CD автоматически деплоит

---

## 📈 Метрики успеха

| Метрика | Текущее | Цель (Фаза 5) |
|---------|---------|---------------|
| Пользователей в день | 10 | 1000+ |
| Ставок в день | 50 | 5000+ |
| Объем торгов (24h) | $100 | $100,000+ |
| Время отклика API | 500ms | <100ms |
| Аптайм | 95% | 99.9%+ |

---

## 🛠 Технические долги

| Долг | Влияние | Когда исправить |
|------|---------|-----------------|
| SQLite → PostgreSQL | Масштабируемость | Фаза 4 |
| Синхронные запросы → Async | Производительность | Фаза 3 |
| Vanilla JS → React/Vue | Поддерживаемость | Фаза 5 |
| Нет тестов → 80% coverage | Надежность | Фаза 2-5 |

---

## 📝 Changelog

### Версия 0.2.0 (Текущая)

**Добавлено:**
- ✅ `useBinanceWebSocket` хук для загрузки истории и WebSocket
- ✅ `VolatilityService` для расчета коэффициентов
- ✅ API endpoints для волатильности
- ✅ Интеграция volatility в betting routes

**Исправлено:**
- ✅ График загружает реальные данные (не шаблон)
- ✅ Коэффициент зависит от волатильности (не таймфрейма)

### Версия 0.1.0 (Предыдущая)

- Базовая интеграция с Polymarket
- Ставки Вверх/Вниз
- Telegram WebApp интеграция

---

## 📞 Контакты

Вопросы и предложения: создайте issue в репозитории.
