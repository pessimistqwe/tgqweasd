# Web3 Integration Documentation

## 📋 Обзор

Этот документ описывает интеграцию EventPredict с блокчейн-инфраструктурой Polymarket на Polygon.

**Статус**: 🟡 Prepared for Integration (MVP готов, Web3 функции в разработке)

---

## 🔗 Контракты Polymarket

### Сеть: Polygon (Chain ID: 137)

| Контракт | Адрес | Назначение |
|----------|-------|------------|
| **USDC_E** | `0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174` | USD Coin (Bridged) - основной стейблкоин для расчётов |
| **CTF** | `0x4D97DCd97eC945f40cF65F87097ACe5EA0476045` | Conditional Tokens Framework - создание рынков |
| **CTF_EXCHANGE** | `0x4bFb41d5B3570DeFd03C39a9A4D8dE6Bd8B8982E` | DEX для торговли акциями исходов |
| **NEG_RISK_CTF_EXCHANGE** | `0xC5d563A36AE78145C45a50134d48A1215220f80a` | Биржа для рынков с негативным риском |

### Ссылки на документацию

- **Официальная документация Polymarket**: https://docs.polymarket.com/
- **Polygon Explorer**: https://polygonscan.com/
- **CTF Documentation**: https://docs.gnosis.io/conditional-tokens/

---

## 📦 Описание контрактов

### 1. USDC_E (USD Coin Bridged)

**Тип**: ERC-20 Token

**Назначение**: Основной расчётный токен платформы. Пользователи покупают/продают акции исходов за USDC.

**Ключевые функции**:
```solidity
function balanceOf(address _owner) external view returns (uint256 balance)
function transfer(address _to, uint256 _value) external returns (bool)
function approve(address _spender, uint256 _value) external returns (bool)
```

**Decimals**: 6

**Использование в проекте**:
- Депозиты пользователей
- Выплаты выигрышей
- Расчёты при торговле акциями

---

### 2. CTF (Conditional Tokens Framework)

**Тип**: Conditional Tokens

**Назначение**: Создание и управление условными токенами (акциями исходов). Каждый рынок предсказаний создаётся через этот контракт.

**Ключевые функции**:
```solidity
function prepareCondition(
    bytes32 questionId,
    uint256 outcomeSlotCount,
    string memory question,
    string[] memory outcomes,
    address oracle
) external

function conditions(bytes32) external view returns (
    address oracle,
    bytes32 questionId,
    uint256 outcomeSlotCount
)
```

**Использование в проекте**:
- Создание новых рынков предсказаний
- Выпуск токенов для каждого исхода (Yes/No)
- Разрешение рынков (определение победителя)

---

### 3. CTF_EXCHANGE

**Тип**: Decentralized Exchange (AMM)

**Назначение**: Автоматическая биржа для торговли акциями исходов. Использует механизм AMM (Automated Market Maker).

**Ключевые функции**:
```solidity
function buy(
    address exchangeToken,
    bytes32 conditionId,
    uint256 outcomeIndex,
    uint256 amount,
    uint256 minOutcomeTokensToBuy
) external

function sell(
    address exchangeToken,
    bytes32 conditionId,
    uint256 outcomeIndex,
    uint256 amount,
    uint256 maxOutcomeTokensToSell
) external
```

**Использование в проекте**:
- Покупка акций исходов (Yes/No)
- Продажа акций обратно в пул ликвидности
- Определение цены на основе спроса/предложения

---

### 4. NEG_RISK_CTF_EXCHANGE

**Тип**: Specialized DEX

**Назначение**: Специализированная биржа для рынков с "негативным риском" - особый тип рынков где механизм торговли отличается от стандартного.

**Использование в проекте**:
- Резервный контракт для специальных типов рынков
- В будущем: поддержка сложных финансовых инструментов

---

## 🏗 Архитектура интеграции

### Текущая архитектура (Web2)

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  Frontend   │────▶│  FastAPI     │────▶│  PostgreSQL │
│  (React)    │◀────│  (Backend)   │◀────│  Database   │
└─────────────┘     └──────────────┘     └─────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  Polymarket  │
                    │  API (Gamma) │
                    └──────────────┘
```

### Целевая архитектура (Web2 + Web3)

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  Frontend   │────▶│  FastAPI     │────▶│  PostgreSQL │
│  (React)    │◀────│  (Backend)   │◀────│  Database   │
└─────────────┘     └──────────────┘     └─────────────┘
       │                    │
       │                    ▼
       │             ┌──────────────┐
       │             │  Polymarket  │
       │             │  API (Gamma) │
       │             └──────────────┘
       │
       │  ┌─────────────────────────────────┐
       └─▶│         Web3 Provider           │
          │  (ethers.js / Web3.py)          │
          └─────────────────────────────────┘
                          │
                          ▼
                   ┌──────────────┐
                   │   Polygon    │
                   │  Blockchain  │
                   │  ┌────────┐  │
                   │  │ USDC   │  │
                   │  │ CTF    │  │
                   │  │ CTF_EX │  │
                   │  └────────┘  │
                   └──────────────┘
```

---

## 📊 Web2 vs Web3 модель

| Характеристика | Web2 (Текущая) | Web3 (Целевая) |
|----------------|----------------|----------------|
| **Хранение средств** | Централизованный баланс в БД | Non-custodial wallet (MetaMask) |
| **Ставки** | Виртуальные очки в БД | Реальные USDC в смарт-контракте |
| **Выплаты** | Админ подтверждает вывод | Автоматически через смарт-контракт |
| **Доверие** | Доверие платформе | Доверие коду (smart contract) |
| **Комиссии** | Нет | Gas fees (Polygon ~$0.01) |
| **Скорость** | Мгновенно | ~2-5 секунд (Polygon) |
| **Регулирование** | Проще | Сложнее (KYC/AML требования) |
| **Доступность** | Telegram Mini App | Любой Web3 кошелёк |

### Плюсы Web2 модели для MVP:
- ✅ Быстрая разработка
- ✅ Нет комиссий для пользователей
- ✅ Простой UX (не нужен криптокошелёк)
- ✅ Мгновенные транзакции
- ✅ Полный контроль для модерации

### Плюсы Web3 модели для production:
- ✅ Non-custodial (пользователи владеют средствами)
- ✅ Прозрачность (все транзакции в блокчейне)
- ✅ Глобальная доступность
- ✅ Интеграция с экосистемой DeFi
- ✅ Доверие через код, а не платформу

---

## 🗺 Roadmap Web3 интеграции

### Этап 1: Подготовка (✅ Выполнено)
- [x] Создан файл конфигурации контрактов
- [x] Добавлены ABI для основных функций
- [x] Созданы заглушки функций Web3
- [x] Документация интеграции

**Время**: 1 день  
**Стоимость**: $0 (внутренние ресурсы)

---

### Этап 2: Подключение кошелька (2-3 дня)
- [ ] Интеграция MetaMask / WalletConnect
- [ ] Переключение на Polygon network
- [ ] Отображение баланса USDC
- [ ] Сохранение адреса кошелька в профиле

**Зависимости**: 
- ethers.js или web3.js библиотека
- Тестовые аккаунты с USDC на Polygon

**Стоимость**: ~$50-100 (тестовые токены)

---

### Этап 3: Чтение из блокчейна (3-4 дня)
- [ ] Чтение баланса USDC пользователя
- [ ] Чтение данных рынков из CTF
- [ ] Чтение истории транзакций
- [ ] Синхронизация с Polymarket API

**Зависимости**: 
- Завершённый Этап 2
- RPC endpoint (public или Infura/Alchemy)

**Стоимость**: $0 (public RPC) или ~$20/мес (Infura)

---

### Этап 4: Запись в блокчейн (5-7 дней)
- [ ] Approval USDC для CTF_EXCHANGE
- [ ] Покупка outcome tokens
- [ ] Продажа outcome tokens
- [ ] Обработка ошибок и revert
- [ ] Отображение статуса транзакции

**Зависимости**: 
- Завершённые Этапы 2-3
- Тестовые токены для gas

**Стоимость**: ~$100-200 (gas fees для тестирования)

---

### Этап 5: Production деплой (3-5 дней)
- [ ] Аудит смарт-контрактов (опционально)
- [ ] Тестирование на mainnet с малыми суммами
- [ ] Мониторинг транзакций
- [ ] Обработка edge cases
- [ ] Документация для пользователей

**Зависимости**: 
- Завершённые Этапы 2-4
- Юридическая консультация (рекомендуется)

**Стоимость**: $0-5000 (аудит опционально) + gas fees

---

## 💰 Оценка полной стоимости

| Этап | Время | Стоимость (без аудита) |
|------|-------|------------------------|
| Этап 1 | 1 день | $0 |
| Этап 2 | 2-3 дня | $50-100 |
| Этап 3 | 3-4 дня | $0-20 |
| Этап 4 | 5-7 дня | $100-200 |
| Этап 5 | 3-5 дней | $0-5000 |
| **Итого** | **14-20 дней** | **$150-5320** |

**Примечание**: Аудит смарт-контрактов опционален для MVP но рекомендован для production с реальными деньгами.

---

## 🛠 Инструкции для разработчиков

### Как активировать Web3 функции

#### 1. Установка зависимостей

```bash
# Frontend (ethers.js)
npm install ethers

# Backend (Web3.py)
pip install web3
```

#### 2. Раскомментировать функции в `web3Config.ts`

```typescript
// Вместо заглушки:
export async function connectWallet(): Promise<string | null> {
  return null;
}

// Использовать реальную реализацию:
export async function connectWallet(): Promise<string | null> {
  if (typeof window.ethereum !== 'undefined') {
    try {
      const accounts = await window.ethereum.request({ 
        method: 'eth_requestAccounts' 
      });
      return accounts[0];
    } catch (error) {
      console.error('Failed to connect wallet:', error);
      return null;
    }
  }
  return null;
}
```

#### 3. Обновить backend (`api/config/polymarket_contracts.py`)

```python
# Вместо заглушки:
def get_usdc_balance(wallet_address: str) -> float:
    return 0.0

# Использовать реальную реализацию:
from web3 import Web3

def get_usdc_balance(wallet_address: str) -> float:
    web3 = Web3(Web3.HTTPProvider(POLYMARKET_CONTRACTS["network_rpc"]))
    contract = web3.eth.contract(
        address=POLYMARKET_CONTRACTS["contracts"]["USDC_E"], 
        abi=USDC_ABI
    )
    balance = contract.functions.balanceOf(wallet_address).call()
    return balance / 1_000_000  # USDC has 6 decimals
```

#### 4. Добавить UI для подключения кошелька

```typescript
// В компоненте Profile
const [walletAddress, setWalletAddress] = useState<string | null>(null);

const handleConnectWallet = async () => {
  const address = await connectWallet();
  if (address) {
    setWalletAddress(address);
    // Сохранить в backend
    await fetch('/api/user/wallet', {
      method: 'POST',
      body: JSON.stringify({ wallet_address: address })
    });
  }
};
```

---

## 🧪 Тестирование

### Тестовая сеть: Polygon Mumbai (Testnet)

**RPC**: https://rpc-mumbai.maticvigil.com/  
**Chain ID**: 80001  
**Faucet**: https://faucet.polygon.technology/

### Тестовые токены

1. Получить тестовый MATIC из faucet
2. Конвертировать MATIC → тестовый USDC через тестовый DEX
3. Тестировать функции покупки/продажи

### Чеклист тестирования

- [ ] Подключение кошелька работает
- [ ] Переключение на Polygon работает
- [ ] Баланс USDC отображается корректно
- [ ] Покупка outcome tokens проходит успешно
- [ ] Продажа outcome tokens проходит успешно
- [ ] Транзакции видны в polygonscan.com
- [ ] Обработка ошибок (недостаточно средств, revert)

---

## 📝 Лицензии и ограничения

### Polymarket API
- API предоставлен для личного использования
- Коммерческое использование требует разрешения
- Source: https://docs.polymarket.com/

### Smart Contracts
- CTF: Gnosis (MIT License)
- USDC: Centre Consortium (Terms of Use apply)
- CTF_EXCHANGE: Polymarket (Proprietary)

### Юридические ограничения
- Polymarket недоступен в некоторых юрисдикциях
- Требуется compliance с местным законодательством
- Рекомендуется консультация юриста перед запуском

---

## 📞 Поддержка

Вопросы по интеграции:
1. Изучить документацию: https://docs.polymarket.com/
2. Проверить контракты на Polygonscan
3. Протестировать на testnet перед mainnet
4. Открыть issue в GitHub репозитории

---

## 📦 Официальные SDK Polymarket (Обновлено: Февраль 2026)

### Интеграция SDK в проект

Проект использует официальные SDK Polymarket для полной интеграции с CLOB API.

#### Backend (Python)

**Установка**:
```bash
pip install py-clob-client
pip install py-builder-signing-sdk  # Опционально: для Builder Program
pip install py-builder-relayer-client  # Опционально: для безгазовых транзакций
```

**Использование**:
```python
from api.services.polymarket_sdk import PolymarketSDK, get_polymarket_sdk

# Проверка доступности
sdk = get_polymarket_sdk()
if sdk.is_configured():
    # Получение данных рынков
    markets = sdk.get_markets(limit=10)
    
    # Размещение ордера
    order = sdk.place_order(
        token_id="12345",
        price=0.55,
        size=100,
        side="BUY",
        order_type="GTC"
    )
    
    # Получение позиций
    positions = sdk.get_positions()
else:
    print("Polymarket SDK not configured. Set API keys in environment.")
```

**Требуемые переменные окружения**:
```bash
POLYMARKET_API_KEY=your_api_key
POLYMARKET_SECRET=your_api_secret
POLYMARKET_PASSPHRASE=your_passphrase
POLYMARKET_PRIVATE_KEY=your_wallet_private_key
POLYMARKET_CHAIN_ID=137  # Polygon mainnet
POLYMARKET_SIGNATURE_TYPE=0  # 0=EOA, 1=Magic Link, 2=Web3 Wallet
```

#### Frontend (TypeScript)

**Установка**:
```bash
npm install @polymarket/clob-client ethers@5
```

**Использование**:
```typescript
import { PolymarketSDK, getPolymarketSDK } from './services/polymarketSDK';

// Инициализация
const sdk = getPolymarketSDK();

// Подключение кошелька
await sdk.connectWallet(privateKey);
// ИЛИ через браузерный кошелёк:
await sdk.connectBrowserWallet();

// Получение данных
if (sdk.isConfigured()) {
    const markets = await sdk.getMarkets();
    const orderbook = await sdk.getOrderbook(tokenId);
    const positions = await sdk.getPositions();
}
```

---

### Таблица SDK со статусом интеграции

| SDK | Язык | Статус | Файл | Описание |
|-----|------|--------|------|----------|
| **CLOB Client** | Python | ✅ Prepared | `api/services/polymarket_sdk.py` | Торговля, ордера, позиции |
| **CLOB Client** | TypeScript | ✅ Prepared | `frontend/services/polymarketSDK.ts` | Торговля, ордера, позиции |
| **Builder Signing SDK** | Python | ⚠️ Placeholder | `api/services/polymarket_sdk.py` | Подпись транзакций |
| **Builder Signing SDK** | TypeScript | ⚠️ Placeholder | `frontend/services/polymarketSDK.ts` | Подпись транзакций |
| **Builder Relayer SDK** | Python | ⚠️ Placeholder | `api/services/polymarket_sdk.py` | Безгазовые транзакции |
| **Builder Relayer SDK** | TypeScript | ⚠️ Placeholder | `frontend/services/polymarketSDK.ts` | Безгазовые транзакции |

**Статусы**:
- ✅ **Prepared** — Wrapper создан, требуется только API key для активации
- ⚠️ **Placeholder** — Методы возвращают заглушки, требуется установка дополнительного SDK
- ❌ **Not Started** — Интеграция не начата

---

### Инструкция по получению API ключей Polymarket

1. **Перейти на** https://reveal.polymarket.com
2. **Войти** через Email/Magic Link или подключить кошелек
3. **Экспортировать приватный ключ** из раздела Settings
4. **Создать API credentials**:
   ```python
   from py_clob_client.client import ClobClient
   
   client = ClobClient("https://clob.polymarket.com", key=private_key, chain_id=137)
   creds = client.create_or_derive_api_creds()
   print(creds)  # { api_key, api_secret, api_passphrase }
   ```
5. **Сохранить** в `.env` файл

---

### Шаги для активации каждой функции

#### 1. Чтение данных рынков (Ready)
- [x] Wrapper создан
- [x] Метод `get_markets()` готов
- [ ] Установить `py-clob-client`
- [ ] Настроить переменные окружения

**Время активации**: 5 минут

#### 2. Размещение ордеров (Ready)
- [x] Wrapper создан
- [x] Метод `place_order()` готов
- [ ] Установить `py-clob-client`
- [ ] Настроить API credentials
- [ ] Настроить allowances (для EOA кошельков)

**Время активации**: 15 минут

#### 3. Отмена ордеров (Ready)
- [x] Wrapper создан
- [x] Метод `cancel_order()` готов
- [ ] Установить `py-clob-client`

**Время активации**: 5 минут

#### 4. Подпись транзакций (Требуется Builder SDK)
- [ ] Установить `py-builder-signing-sdk`
- [ ] Импортировать в `polymarket_sdk.py`
- [ ] Реализовать метод `sign_transaction()`

**Время активации**: 1-2 часа

#### 5. Безгазовые транзакции (Требуется Relayer SDK)
- [ ] Установить `py-builder-relayer-client`
- [ ] Импортировать в `polymarket_sdk.py`
- [ ] Реализовать метод `submit_gasless_transaction()`

**Время активации**: 1-2 часа

---

### Оценка времени на полную интеграцию

| Компонент | Время | Сложность |
|-----------|-------|-----------|
| CLOB Client (чтение данных) | 5 минут | 🟢 Low |
| CLOB Client (ордера) | 15 минут | 🟡 Medium |
| Builder Signing SDK | 1-2 часа | 🟠 High |
| Builder Relayer SDK | 1-2 часа | 🟠 High |
| Тестирование | 2-4 часа | 🟡 Medium |
| **Итого** | **4-8 часов** | |

---

## 📞 Поддержка

Вопросы по интеграции:
1. **Документация API**: [POLYMARKET_API_REFERENCE.md](./POLYMARKET_API_REFERENCE.md)
2. **Официальная документация**: https://docs.polymarket.com/
3. **GitHub Organization**: https://github.com/Polymarket
4. **Discord**: https://discord.gg/polymarket

---

**Последнее обновление**: 19 февраля 2026  
**Статус**: ✅ SDK Integrated (Prepared for Activation)  
**Следующий этап**: Настройка API ключей и тестирование
