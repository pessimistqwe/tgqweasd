# Инструкция по тестированию

## Проверка исправлений графика и коэффициентов

### Предварительные требования

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите сервер:**
   ```bash
   cd api
   uvicorn index:app --reload --host 0.0.0.0 --port 8000
   ```

3. **Откройте Telegram WebApp** или перейдите на `http://localhost:8000`

---

## ✅ ТЕСТ 1: Проверка графика (История + WebSocket)

### Цель
Убедиться, что график загружает реальные исторические данные из Binance API, а не показывает "шаблон".

### Шаги

1. **Откройте событие с крипто-тематикой:**
   - Найдите событие со словами "Bitcoin", "Ethereum", "BTC", "ETH"
   - Нажмите на событие для открытия деталей

2. **Проверьте загрузку графика:**
   - Откройте Developer Tools (F12) → Console
   - Ищите сообщение: `Binance WebSocket connected: btcusdt@trade`
   - Проверьте Network tab → запрос к `https://api.binance.com/api/v3/klines`

3. **Сравните данные:**
   - Откройте новую вкладку: https://api.binance.com/api/v3/klines?symbol=BTCUSDT&interval=15m&limit=96
   - Сравните последнюю цену из API с ценой на графике
   - **Ожидается:** Цены совпадают (разница ≤ 0.1%)

4. **Проверьте обновление в реальном времени:**
   - Наблюдайте за графиком 30-60 секунд
   - Цена должна обновляться при новых сделках
   - В консоли должны появляться сообщения об обновлениях

5. **Проверьте смену таймфрейма:**
   - Нажмите кнопки "1m", "5m", "1h" на графике
   - При смене таймфрейма должен быть новый запрос к Binance API
   - Проверьте Network tab — запросы с разными `interval=`

### Критерии успеха

- [ ] График показывает реальные цены (совпадают с Binance)
- [ ] WebSocket подключается и обновляет цену
- [ ] При смене таймфрейма загружаются новые данные
- [ ] Нет "шаблонных" или "фейковых" данных

### Возможные проблемы

| Проблема | Решение |
|----------|---------|
| График не загружается | Проверьте доступ к Binance API (нет блокировки) |
| WebSocket не подключается | Проверьте firewall/антивирус |
| Цены не совпадают | Убедитесь, что событие распознано правильно (BTC, ETH) |

---

## ✅ ТЕСТ 2: Проверка коэффициентов от волатильности

### Цель
Убедиться, что коэффициент зависит от реальной волатильности рынка, а не от таймфрейма графика.

### Шаги

1. **Проверьте API волатильности:**
   ```bash
   curl http://localhost:8000/api/volatility/odds/BTCUSDT
   ```
   
   **Ожидаемый ответ:**
   ```json
   {
     "symbol": "BTCUSDT",
     "volatility": 0.45,
     "odds": 1.92,
     "cached": false,
     "timestamp": "2026-02-18T12:00:00",
     "price_count": 6,
     "min_price": 52340.50,
     "max_price": 52580.25,
     "current_price": 52450.00
   }
   ```

2. **Проверьте конфигурацию:**
   ```bash
   curl http://localhost:8000/api/volatility/config
   ```
   
   Убедитесь, что пороги волатильности настроены:
   - Low: < 0.5% → odds 1.90-1.95
   - Medium: 0.5%-2% → odds 1.80-1.90
   - High: > 2% → odds 1.50-1.80

3. **Сравните коэффициенты для разных таймфреймов:**
   - Откройте график BTC
   - Выберите таймфрейм "1m" → запомните коэффициент
   - Выберите таймфрейм "1h" → проверьте коэффициент
   - **Ожидается:** Коэффициенты ОДИНАКОВЫЕ (зависят от волатильности, не от таймфрейма)

4. **Проверьте обновление коэффициентов:**
   - Подождите 30 секунд (интервал обновления)
   - Снова запросите API: `curl http://localhost:8000/api/volatility/odds/BTCUSDT`
   - Проверьте, что `cached: true` (если прошло < 30 сек)

5. **Проверьте для разных символов:**
   ```bash
   curl "http://localhost:8000/api/volatility/odds?symbols=BTCUSDT,ETHUSDT,SOLUSDT"
   ```
   
   Убедитесь, что каждый символ имеет свой коэффициент в зависимости от волатильности.

### Критерии успеха

- [ ] Коэффициент рассчитывается от волатильности
- [ ] Коэффициент НЕ зависит от таймфрейма графика
- [ ] Коэффициент обновляется каждые 30 секунд
- [ ] Разные символы имеют разные коэффициенты

### Формула проверки

```
Волатильность = (std_dev / mean) * 100

Если volatility < 0.5%:
    odds = 1.90 - 1.95
Если 0.5% <= volatility < 2%:
    odds = 1.80 - 1.90
Если volatility >= 2%:
    odds = 1.50 - 1.80
```

---

## ✅ ТЕСТ 3: Проверка админ-панели

### Цель
Убедиться, что новые endpoints админ-панели работают корректно.

### Шаги

1. **Проверьте статистику платформы:**
   ```bash
   curl "http://localhost:8000/api/admin/stats/platform?telegram_id=1972885597"
   ```
   
   **Ожидаемый ответ:**
   ```json
   {
     "total_users": 10,
     "active_users_24h": 5,
     "total_events": 50,
     "active_events": 30,
     "resolved_events": 20,
     "total_volume": 10000.00,
     "total_commission_earned": 300.00,
     "pending_withdrawals": 2,
     "blocked_users": 1
   }
   ```

2. **Проверьте активные ставки:**
   ```bash
   curl "http://localhost:8000/api/admin/bets/active?telegram_id=1972885597&limit=10"
   ```

3. **Проверьте комиссию платформы:**
   ```bash
   curl "http://localhost:8000/api/admin/commission?telegram_id=1972885597"
   ```
   
   Должно вернуть: `{"commission_percent": 3.0, ...}`

4. **Обновите комиссию:**
   ```bash
   curl -X POST "http://localhost:8000/api/admin/commission?telegram_id=1972885597" \
     -H "Content-Type: application/json" \
     -d '{"commission_percent": 5.0}'
   ```

5. **Проверьте ожидающие выводы:**
   ```bash
   curl "http://localhost:8000/api/admin/withdrawals/pending?telegram_id=1972885597"
   ```

### Критерии успеха

- [ ] Статистика возвращает корректные данные
- [ ] Активные ставки отображаются
- [ ] Комиссия обновляется
- [ ] Выводы показываются

---

## ✅ ТЕСТ 4: Проверка WebSocket реконнекта

### Цель
Убедиться, что WebSocket автоматически переподключается при обрыве.

### Шаги

1. Откройте график BTC
2. Откройте Network tab в DevTools
3. Найдите WebSocket подключение к `stream.binance.com`
4. Правой кнопкой → Stop (или закройте вкладку Binance в firewall)
5. Подождите 5 секунд
6. **Ожидается:** Автоматическое переподключение

### Критерии успеха

- [ ] WebSocket переподключается через 5 секунд
- [ ] График продолжает обновляться

---

## ✅ ТЕСТ 5: Нагрузочный тест графика

### Цель
Проверить производительность при частых обновлениях.

### Шаги

1. Откройте 5-10 событий с графиками одновременно
2. Наблюдайте за потреблением памяти в DevTools → Performance
3. Проверьте FPS (должен быть > 30)

### Критерии успеха

- [ ] FPS > 30
- [ ] Нет утечек памяти
- [ ] График не "лагает"

---

## 🐛 Отладка

### Логирование на сервере

При запуске сервера включите подробное логирование:

```bash
uvicorn index:app --reload --log-level debug
```

Ищите сообщения:
- `Volatility calculated for BTCUSDT: volatility=X%, odds=Yx`
- `Binance WebSocket connected: ...`

### Логирование на клиенте

Откройте Console в DevTools и ищите:
- `Binance WebSocket connected: ...`
- `Error loading chart data: ...`
- `Reconnecting Binance WebSocket...`

### Проверка кэша волатильности

```bash
# Закэшированные данные (быстрый ответ)
curl http://localhost:8000/api/volatility/cache/BTCUSDT

# Статус сервиса
curl http://localhost:8000/api/volatility/status
```

---

## 📊 Чек-лист для acceptance

### График

- [ ] Загружает реальные исторические свечи
- [ ] WebSocket обновляет в реальном времени
- [ ] Смена таймфрейма работает корректно
- [ ] Авто-реконнект при обрыве
- [ ] Производительность > 30 FPS

### Коэффициенты

- [ ] Зависят от волатильности
- [ ] НЕ зависят от таймфрейма
- [ ] Обновляются каждые 30 секунд
- [ ] Разные символы → разные коэффициенты

### Админ-панель

- [ ] Просмотр активных ставок
- [ ] Разрешение событий
- [ ] Настройка комиссии
- [ ] Бан пользователей
- [ ] Статистика доходов

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Проверьте Console браузера
3. Убедитесь, что Binance API доступен
4. Проверьте переменные окружения

Создайте issue в репозитории с:
- Шагами для воспроизведения
- Ожидаемым результатом
- Фактическим результатом
- Скриншотами/логами
