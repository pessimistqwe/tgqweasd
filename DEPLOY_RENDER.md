# 🚀 Деплой на Render.com (БЕСПЛАТНО)

Railway ограничили бесплатный тариф, поэтому переходим на Render.com — там нет ограничений по времени работы!

---

## 📋 Шаг 1: Регистрация на Render

1. Перейди на https://render.com
2. Нажми **Sign Up**
3. Войди через **GitHub** (рекомендуется) или email
4. Подтверди email

---

## 📋 Шаг 2: Создание Web Service

### Вариант А: Через Dashboard (рекомендуется)

1. Нажми **New +** → **Web Service**
2. Выбери свой репозиторий: `pessimistqwe/tgqweasd`
3. Заполни настройки:

```
Name: eventpredict
Region: Frankfurt (eu-central)
Branch: main
Root Directory: (оставь пустым)
Runtime: Python
Build Command: pip install -r api/requirements.txt
Start Command: cd api && uvicorn index:app --host 0.0.0.0 --port $PORT
```

4. Выбери тариф:
   - **Free** — бесплатно, 750 часов/месяц
   - (Если нужно 24/7 — можно позже апгрейднуть за $7/мес)

5. Нажми **Advanced** и добавь переменные окружения:

```
PORT=10000
TEST_DB_PATH=/tmp/events.db
DISABLE_SCHEDULER=0
POLYMARKET_SYNC_INTERVAL=7200
POLYMARKET_VERBOSE_LOGS=0
```

6. Нажми **Create Web Service**

---

### Вариант Б: Через render.yaml (автоматически)

1. В репозитории уже есть файл `render.yaml`
2. На Render нажми **New +** → **Blueprint**
3. Выбери репозиторий
4. Render автоматически применит настройки из `render.yaml`
5. Нажми **Apply**

---

## 📋 Шаг 3: Проверка деплоя

1. После создания сервиса начнётся деплой (2-5 минут)
2. Логи деплоя: вкладка **Logs**
3. Когда увидишь `Application started` — готово!
4. Твой URL: `https://eventpredict-xxxx.onrender.com`

---

## 📋 Шаг 4: Проверка работы

1. Открой URL сервиса
2. Проверь что загрузилась главная страница
3. Проверь баланс (должен быть 0)
4. Открой любое событие

---

## 🔧 Переменные окружения (опционально)

Для полной функциональности добавь:

```
CRYPTOBOT_API_TOKEN=your_token
ADMIN_TELEGRAM_IDS=123456789,987654321
```

---

## ⚠️ Важные моменты

### Free тариф:
- ✅ 750 часов/месяц бесплатно
- ✅ Автоматический деплой из GitHub
- ⚠️ Засыпает после 15 минут бездействия
- ⚠️ При первом запросе "просыпается" (30 сек)

### Как избежать засыпания:
1. **UptimeRobot** (бесплатно):
   - https://uptimerobot.com
   - Создай monitor на твой URL
   - Ping каждые 5 минут
   - Сервис не будет засыпать!

2. **Или платный тариф** ($7/мес):
   - Не засыпает
   - Больше ресурсов

---

## 🔄 Авто-деплой

Render автоматически деплоит при:
- Push в ветку `main`
- Merge pull request
- Ручном триггере через Dashboard

---

## 🆘 Troubleshooting

### Ошибка: "Build failed"
```
→ Проверь логи во вкладке Logs
→ Убедись что requirements.txt в корне
→ Проверь что Start Command правильный
```

### Ошибка: "Module not found"
```
→ Проверь импорты в api/index.py
→ Убедись что все пакеты в requirements.txt
```

### Ошибка: "Database error"
```
→ Проверь TEST_DB_PATH=/tmp/events.db
→ Убедись что есть права на запись в /tmp
```

### Сервис "засыпает"
```
→ Это нормально для free тарифа
→ Используй UptimeRobot для ping
→ Или апгрейдни до платного
```

---

## 📊 Мониторинг

- **Logs** — логи приложения
- **Metrics** — CPU, память, запросы
- **Events** — история деплоев

---

## ✅ Готово!

Твой проект работает на Render.com!

**Следующие шаги:**
1. Добавь URL в Telegram Bot → Bot Settings → Menu Button
2. Протестируй все функции
3. Настрой CryptoBot для платежей

---

**Поддержка:** Если что-то не работает — открой issue на GitHub или напиши в Telegram.
