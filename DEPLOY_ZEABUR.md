# 🚀 Деплой на Zeabur

**Zeabur** — это платформа для деплоя приложений с автоматическим масштабированием и простой настройкой.

## Быстрый старт

### 1. Подготовка

Убедитесь что у вас есть:
- Аккаунт на [Zeabur](https://zeabur.com)
- GitHub репозиторий с кодом
- Telegram Bot Token (от @BotFather)

### 2. Деплой через Dashboard (рекомендуется)

#### Шаг 1: Создание проекта

1. Войдите в [Zeabur Dashboard](https://app.zeabur.com)
2. Нажмите **"New Project"**
3. Введите имя проекта (например, `eventpredict`)
4. Нажмите **"Create"**

#### Шаг 2: Добавление сервиса

1. В проекте нажмите **"New"** → **"GitHub"**
2. Выберите репозиторий `yourusername/eventpredict`
3. Zeabur автоматически определит что это Docker проект

#### Шаг 3: Настройка Build

```
Build Command: (оставьте пустым - используется Dockerfile)
Start Command: (оставьте пустым - используется из Dockerfile)
```

#### Шаг 4: Добавление переменных окружения

Нажмите **"Variables"** и добавьте:

| Ключ | Значение | Описание |
|------|----------|----------|
| `PORT` | `8000` | Порт сервера |
| `TELEGRAM_BOT_TOKEN` | `your_bot_token` | Токен бота от @BotFather |
| `ADMIN_TELEGRAM_ID` | `123456789` | Ваш Telegram ID |
| `TEST_DB_PATH` | `/tmp/events.db` | Путь к SQLite БД |
| `POLYMARKET_SYNC_INTERVAL` | `7200` | Интервал синхронизации (сек) |
| `SECRET_KEY` | `random_string` | Секретный ключ |

#### Шаг 5: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (~2-3 минуты)
3. Скопируйте URL сервиса

### 3. Деплой через CLI (альтернатива)

```bash
# Установка Zeabur CLI
npm install -g zeabur

# Авторизация
zeabur login

# Деплой
cd c:\Users\user\Desktop\tgqweasd
zeabur deploy
```

### 4. Добавление PostgreSQL (опционально)

Для production рекомендуется использовать PostgreSQL вместо SQLite:

1. В проекте нажмите **"New"** → **"Database"** → **"PostgreSQL"**
2. Дождитесь создания БД
3. Скопируйте `DATABASE_URL` из вкладки Variables
4. Добавьте переменную `DATABASE_URL` в сервис

### 5. Проверка работы

#### Health Check

Откройте в браузере:
```
https://your-app.zeabur.app/health
```

Должно вернуться:
```json
{"status": "healthy"}
```

#### API Documentation

```
https://your-app.zeabur.app/docs
```

### 6. Интеграция с Telegram

1. Откройте @BotFather в Telegram
2. Отправьте `/newapp` или выберите существующего бота
3. Укажите Web App URL: `https://your-app.zeabur.app`
4. Готово!

### 7. Мониторинг

#### Логи

В Dashboard Zeabur:
1. Выберите сервис
2. Откройте вкладку **"Logs"**
3. Смотрите логи в реальном времени

#### Метрики

1. Откройте вкладку **"Metrics"**
2. Проверяйте использование CPU, памяти, запросы

### 8. Обновление деплоя

#### Автоматически (рекомендуется)

1. Включите **Auto Deploy** в настройках сервиса
2. При каждом push в GitHub будет автоматический деплой

#### Вручную

```bash
git push origin main
# Zeabur автоматически задеплоит
```

Или через Dashboard:
1. Откройте сервис
2. Нажмите **"Redeploy"**

### 9. Troubleshooting

#### Ошибка: "Container exited with code 1"

Проверьте логи:
```
Смотрите вкладку Logs в Dashboard
```

Возможные причины:
- Неверный `TELEGRAM_BOT_TOKEN`
- Ошибка в коде (проверьте через `docker build` локально)

#### Ошибка: "Port not found"

Убедитесь что приложение слушает порт 8000:
```python
uvicorn index:app --host 0.0.0.0 --port 8000
```

#### Ошибка: "Database not found"

Проверьте переменные окружения:
- `TEST_DB_PATH` должен быть `/tmp/events.db`
- Или используйте `DATABASE_URL` для PostgreSQL

### 10. Оптимизация затрат

Zeabur предоставляет бесплатный тариф с лимитами:
- 500 часов в месяц
- 1GB RAM
- 1 CPU

Для оптимизации:
1. Используйте SQLite вместо PostgreSQL (для небольших проектов)
2. Отключите scheduler если не нужен: `DISABLE_SCHEDULER=1`
3. Увеличьте интервал синхронизации: `POLYMARKET_SYNC_INTERVAL=14400`

---

## Пример конфигурации

### Production настройки

```env
PORT=8000
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_TELEGRAM_ID=1972885597
DATABASE_URL=postgresql://user:pass@host:5432/dbname
SECRET_KEY=your_secret_key_here
POLYMARKET_SYNC_INTERVAL=7200
DISABLE_SCHEDULER=0
LOG_LEVEL=INFO
```

### Development настройки

```env
PORT=8000
TELEGRAM_BOT_TOKEN=test_token
ADMIN_TELEGRAM_ID=123456789
TEST_DB_PATH=/tmp/events.db
SECRET_KEY=dev_secret
POLYMARKET_SYNC_INTERVAL=3600
LOG_LEVEL=DEBUG
```

---

## Поддержка

При возникновении проблем:
1. Проверьте [документацию Zeabur](https://docs.zeabur.com)
2. Посмотрите логи в Dashboard
3. Откройте issue в GitHub репозитории
